{
  "name": "finvisiq-frontend",
  "version": "1.0.0",
  "description": "FinvisIQ — Personal Finance Intelligence Platform Web Frontend (Netlify)",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && node -e \"const fs=require('fs'); fs.cpSync('js', 'dist/js', {recursive: true});\"",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.2.11"
  }
}
