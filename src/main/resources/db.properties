# Database Configuration for FinvisIQ (MySQL)
# Production credentials are provided via environment variables (DB_URL, DB_USERNAME, DB_PASSWORD)
db.driver=com.mysql.cj.jdbc.Driver
db.url=${DB_URL}
db.username=${DB_USERNAME}
db.password=${DB_PASSWORD}
db.name=smart_finance_db
