package com.smartfinance; import com.smartfinance.dao.*; import com.smartfinance.model.*; import java.time.LocalDate; public class Scratch { public static void main(String[] args) { TransactionDAO dao = new TransactionDAO(); Transaction t = new Transaction(1, 150.0, "INCOME", "Salary", LocalDate.now(), "Test"); int id = dao.insert(t); System.out.println("Inserted: " + id); System.out.println("Total: " + dao.findByUserId(1).size()); } }
